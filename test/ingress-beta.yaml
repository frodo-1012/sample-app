apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-beta
  namespace: test
  labels:
    test: test-beta
spec:
  rules:
    - host: test.com
      http:
        paths:
          - path: /
            backend:
              serviceName: test
              servicePort: 8080
