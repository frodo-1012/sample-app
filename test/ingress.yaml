apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test
  namespace: test
  labels:
    app: argocd-rollout
    test: test22
    argocd.argoproj.io/instance: dev-test-rollout
spec:
  rules:
    - host:
      http:
        paths:
          - path: /*
            backend:
              serviceName: test
              servicePort: 80
