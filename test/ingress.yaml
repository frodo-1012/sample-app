apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: test
  namespace: test
  labels:
    app: argocd-rollout
    test: test2
    argocd.argoproj.io/instance: dev-test-rollout
spec:
  rules:
    - host: junebee-sample-app.daumtools.com
      http:
        paths:
          - path: /*
            backend:
              serviceName: test
              servicePort: 80
